# Copy to .env.dev and adjust for local runs
SPRING_PROFILES_ACTIVE=dev
# Use a 32+ byte secret. Generate with: openssl rand -base64 48
JWT_SECRET=replace-with-a-strong-secret-value
FRONTEND_ORIGINS=http://localhost:3000
ML_SERVICE_URL=http://localhost:5001
CHATBOT_SERVICE_URL=http://localhost:8000
